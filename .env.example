# Supabase (optional for V1) — if these are missing the app runs entirely in
# local demo mode (data stored in the browser via localStorage). See supabase/schema.sql
# for the tables to create once you're ready to wire up real persistence + auth.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
